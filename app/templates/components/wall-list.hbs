{{yield}}
<div class="walls">
{{#each walls as |wall|}}
  <div class="wall--name">{{wall.name}}</div>
  {{device-list devices=wall.devices}}
  {{#link-to 'device.new' wall.id}}Add Device{{/link-to}}
{{/each}}
</div>
